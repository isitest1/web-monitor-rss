// Re-exported from @web-monitor/shared so the Runner (Playwright) and the
// Chrome extension's local check classify statuses identically (§8.3).
export { classifyHttpStatus, classifyException } from '@web-monitor/shared';
