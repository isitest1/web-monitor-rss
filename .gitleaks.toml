title = "gitleaks config for web-monitor-rss"

[extend]
useDefault = true

[allowlist]
description = "Deliberately embedded Extension API token for single-user convenience (see apps/extension/src/lib/default-config.ts for the rationale); also present in the committed, built extension output that bundles it"
paths = [
  '''apps/extension/src/lib/default-config\.ts''',
  '''apps/extension/dist/background\.js''',
  '''apps/extension/dist/options\.js''',
]
